---
description: Find, inspect, or create the benchmark behind a metric or answer path.
---

# Benchmark

Alias for `/chatdata:build-benchmark`.

Use this when the customer asks for a benchmark, trusted SQL path, raw SQL SoT, verified dashboard/report tie-out, or source-of-truth comparison. If customer-approved SQL or a verified dashboard already exists, inspect it before drafting a new benchmark.
