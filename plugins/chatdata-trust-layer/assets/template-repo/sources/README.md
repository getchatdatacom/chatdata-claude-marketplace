# Source References

Write one source reference per domain or canonical dataset. ChatData uses these files after approved metric packets and answer paths, and before raw SQL fallback.

Start from `domain-reference-template.md`, then replace placeholders with the customer's real grain, scope, filters, owners, tables, dashboards, gotchas, and cross-references.
